type Query {
  "Returns all the guest"
  getGuests: [Guest]
  "Returns a guest"
  getGuest(id: ID!): Guest
}

type GuestInput {
  name: String!
  state: String!
}

type Mutation {
  "Creates a new guest"
  createGuest: String
}

type Guest {
  _id: ID!
  name: String!
  state: String!
}
